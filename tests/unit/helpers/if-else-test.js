
import { ifElse } from 'flickr-search/helpers/if-else';
import { module, test } from 'qunit';

module('Unit | Helper | if else');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifElse([42]);
  assert.ok(result);
});

