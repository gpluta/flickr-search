import Ember from 'ember';

export default Ember.Component.extend({
  // Parameters passed to component
  photoTitle: null,
  photoFarm: null,
  photoServer: null,
  photoId: null,
  photoSecret: null


});
